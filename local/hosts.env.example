# Private SSH hosts for scripts/pull_server_dumps.sh
# Copy to hosts.env (gitignored) and fill in.

MW_SQL_HOST=sql.example.invalid
MW_SQL_PATH=/var/backups/mysql/sqldump
DRUPAL_HOST=drupal.example.invalid
